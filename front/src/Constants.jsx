import React from 'react';

export const MAX_WORK_MINUTES = 60;
export const MAX_SHORT_BREAK_MINUTES = 30;
export const MAX_LONG_BREAK_MINUTES = 50;


export const localKeys = {
  numUntilLongBreak: "NUM_UNTIL_LONG_BREAK"
};

export const DEFAULT_NUM_UNTIL_LONG_BREAK = 4;

export const timeTypes = {
  work: "WORK",
  shortBreak: "SHORT_BREAK",
  longBreak: "LONG_BREAK"
}